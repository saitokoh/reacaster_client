{
  "name": "reacaster_client",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "dependencies": {
    "electron-node-config": "^1.1.0",
    "jquery": "^3.5.1",
    "js-yaml": "^3.14.0",
    "socket.io-client": "^2.3.0"
  },
  "devDependencies": {
    "electron": "^9.2.0",
    "electron-builder": "^22.8.0",
    "electron-packager": "^15.0.0"
  },
  "build": {
    "appId": "yurut",
    "win": {
      "target": "zip"
    },
    "directories": {
      "output": "dist"
    }
  },
  "scripts": {
    "package": "electron-forge package",
    "pack": "electron-packager . reacaster_client --platform=darwin,win32 --arch=x64  --overwrite --app-version=1.0.0",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
